<%= form_with model: @task, url: tasks_update_path, method: :patch, local: true do |f| %>
  <%= f.text_field :name, autocomplete: :off %>
  <%= f.text_area :description, autocomplete: :off %>
  <%= f.date_field :deadline %>
<% end %>